tunnel: ${CF_GRAFANA_TUNNEL_ID}
credentials-file: /etc/cloudflared/credentials.json

ingress:
  - hostname: ${CF_GRAFANA_HOSTNAME}
    service: http://grafana-auth-proxy:3002
  - service: http_status:404
