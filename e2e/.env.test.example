# E2E Test Environment
# Copy to .env.test and customize for your environment.
# These values match the defaults from the root .env.example.

# Base URL for the MFE shell (frontend)
BASE_URL=http://localhost:3000

# Seeded admin credentials (must match `just seed-admin` values)
ADMIN_EMAIL=admin@gofin.local
ADMIN_PASSWORD=Admin1234!
